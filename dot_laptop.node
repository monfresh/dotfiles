#!/bin/sh

set -e

if command -v prettier >/dev/null; then
  success_echo "Prettier is already installed. Skipping ..."
else
  info_echo "Installing prettier globally ..."
  NODENV_VERSION=16.13.1 yarn global add prettier
fi
